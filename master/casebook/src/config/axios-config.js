export const axiosDefaultConfig = {
  baseURL: "http://localhost:3000", // 默认请求路径
  timeout: 100000, // 设置请求超时时间
};

export const axiosAfterConfig = {
  baseURL: "http://localhost:3000/after", // 默认请求路径
  timeout: 100000, // 设置请求超时时间
};


export const axiosFrontConfig = {
  baseURL: "http://localhost:3000/front", // 默认请求路径
  timeout: 100000, // 设置请求超时时间
};
